import { Button, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function menu() {
	return (
		<SafeAreaView>
			<Text>A menu page che</Text>
		</SafeAreaView>
	);
}
